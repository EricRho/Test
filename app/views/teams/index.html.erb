<p id="notice"><%= notice %></p>

<h1>Listing Teams</h1>

<!-- <table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @teams.each do |team| %>
      <tr>
        <td><%= team.name %></td>
        <td><%= link_to 'Show', team %></td>
        <td><%= link_to 'Edit', edit_team_path(team) %></td>
        <td><%= link_to 'Destroy', team, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
 -->

 <h3 ng-controller='clockCtrl'>{{ clock }}</h3>

 <table class='table table-striped'>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan='3'></th>
    </tr>
  </thead>

  <tbody ng-controller='teamsCtrl'>
    <tr ng-repeat='team in teams'>
      <td>
        {{ team.name }}
      </td>
    </tr>
  </tbody>
 </table>
<br>

<%= link_to 'New Team', new_team_path %>
